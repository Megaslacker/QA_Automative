package HomeTasks;
/*
public class temp {
    public static void main(String[] args) {
        int size = 10;
        int step = 2;
        int[] workingArray = CreateArrayLists.createRandomArray(10);
        int[] newWorkingArray = new int[size];

        for (int i = 0; i < size; i++) {
            int newIndex = i + step;
            if (newIndex < size) {
                newWorkingArray[newIndex] = workingArray[i];
            } else {
                newWorkingArray[newIndex - size] = workingArray[i];
            }
        }
        System.out.println("newWorkingArray: ");
        CreateArrayLists.showArray(newWorkingArray);

    }
}
*/